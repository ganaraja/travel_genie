[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --cov=core
    --cov=tools
    --cov=agent
    --cov-report=term-missing
    --cov-report=html
